Frontend workplan:

1. Map backend API endpoints (server.js and routes).
2. Scaffold minimal static frontend pages:
   - `login.html` (if backend requires auth)
   - `profile.html` — view & edit profile
   - `payment.html` — make payment form
   - `payments.html` — user payment history
3. Add `scripts/api.js` with helper fetch functions (handles cookies/session-based auth)
4. Add basic styles `styles.css`.
5. Manual smoke test instructions.

I'll start by mapping backend endpoints.